# Назначение: открывает backend pod'ы как внутренний Kubernetes Service на порту 8000.
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: wine-app
  labels:
    app: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
