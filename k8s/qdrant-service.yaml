# Назначение: открывает pod'ы Qdrant как Kubernetes Service на порту 6333.
apiVersion: v1
kind: Service
metadata:
  name: qdrant
  namespace: wine-app
spec:
  selector:
    app: qdrant
  ports:
    - name: http
      port: 6333
      targetPort: 6333
