# Назначение: разворачивает pod'ы backend API в namespace wine-app.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: wine-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: darckshot/backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: backend-secrets
          resources:
            # CPU задается в millicores: 100m = 0.1 vCPU, 1000m = 1 vCPU.
            requests:
              cpu: "500m"
              memory: "768Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
